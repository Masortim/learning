#include <iostream>
#include <Windows.h>

int main()
{
	double item;                // double - вещественная переменная двойной точности с плавающей точкой, item - переменная
	item = 10.12304;            // значение переменной (данный код выведет 10.123040, поскольку double добавляет дополнительные нули, если число меньше 9 символов)
	printf("%f\n", item);       // десятичное число (f) с плавающей точкой без указания длыны строки
	printf("%10f\n", item);     // f с длиной строки 10 символов (добавит дополнительный пробел слева, т. к. символов 9)
	printf("%012f\n", item);    // строка из 12 символов, но вместо пробела будет 0 (получится 00010.123040); работает только с числами
	system("pause");
	return 0;
}
